#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * malloc checked - A function that checks memory using malloc
 * @b: integer variable to be checked
 * Return: 0
 */

void *malloc_checked(unsigned int b);
{
	void *a;

	a = malloc(b);
	if (t == NULL)
		exit(98);

	return (a);
}
